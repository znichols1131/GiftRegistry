@using Microsoft.AspNet.Identity



@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()

        <div class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDarkDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                <img id="profilePictureDropdown" class="profile-picture-dropdown" src="" alt="Dropdown Menu" />Me
            </a>

            <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">

                @if (User.Identity.IsAuthenticated)
                {<a class="dropdown-item" href="@Url.Action("Edit", "Person")">Account Info</a>}
                <a class="dropdown-item" href="@Url.Action("Index", "Manage", new { title = "Manage" })">Security</a>
                @if (User.Identity.IsAuthenticated)
                {<a class="dropdown-item" href="@Url.Action("Index", "Notification")">Notifications</a>}
                @if (User.Identity.IsAuthenticated)
                {<a class="dropdown-item" href="@Url.Action("Index", "Transaction")">Transactions</a>}
                <a class="dropdown-item" href="javascript:document.getElementById('logoutForm').submit()">Log Off</a>
            </div>
        </div>

        @*<ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page"
                       href="@Url.Action("Index", "Manage", new { title = "Manage" })">
                        @string.Format("Hello " + User.Identity.GetUserName() + "!")
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="javascript:document.getElementById('logoutForm').submit()">Log off</a>
                </li>
            </ul>*@
    }
}
else
{
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link active" aria-current="page"
               href="@Url.Action("Register", "Account", new { id = "registerLink" })">Register</a>
        </li>
        <li class="nav-item">
            <a class="nav-link active" aria-current="page"
               href="@Url.Action("Login", "Account", new { id = "loginLink" })">Log in</a>
        </li>
    </ul>
}

@Scripts.Render("~/bundles/jquery")

<script type="text/javascript">

    $(document).ready(function () {

        $.ajax({
                type: "GET",
                 url: "@(Url.Action("GetProfilePicture", "Account"))",
                contentType: "application/json; charset=utf-8",
                success: function (data) {
                    var imageData = data.imageSrc;
                    $('#profilePictureDropdown').attr('src', imageData);
                },
                error: function () {
                    alert("Could not load profile picture.");
                }
        });

    });

</script>

<style>
    .profile-picture-dropdown {
        height: 30px;
        border-radius: 50%;
        margin-right: 10px;
    }

</style>