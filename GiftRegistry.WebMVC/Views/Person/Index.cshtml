@model IEnumerable<GiftRegistry.Models.PersonListItem_Stranger>

@{
    ViewBag.Title = "Add Friend";
}

<h2>Add Friend</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FullName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FullName)
            </td>
            <td>
                @Html.ActionLink("Add Friend", "Create", "Friend", new { id = item.PersonID, name = item.FullName }, null)
            </td>
        </tr>
    }

</table>
